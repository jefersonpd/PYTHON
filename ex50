# Requisitos:
# 1 - Leia 6 números inteiros
# 2 - Some e mostre o valor dos números pares

print('Você deve escolher seis número. \n')
lista = []
for n in range (1, 7):
    num = int(input('Escolha o {}º número: ' .format(n)))
    if num % 2 == 0:
        lista.append(num)

print(sum(lista))
